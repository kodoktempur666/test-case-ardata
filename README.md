# TEST 2

## Client Setup

First, run the development server:

```bash
cd client

npm install

npm run dev
```

## Database Setup

First, run the development server:

```bash
cd server

npm install

npm run db:generate

npm run db:migrate

npm run dev
```
